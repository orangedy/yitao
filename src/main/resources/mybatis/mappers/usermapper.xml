<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.netease.shijin.yitao.mapper.UserMapper">
	<select id="isUserExist">
		select 1 from user where accountID = #{accountID}
		limit 1
	</select>
	<update id="updateUserInfo">
		update user set nickname = #{nickName}, iconurl =
		#{iconURL} where accountID = #{accountID}
	</update>
	<insert id="addUser">
		insert into
		user(nickname,accountid,iconurl)
		values(#{nickName},#{accountID},#{iconURL})
	</insert>
	<select id="getUserID">
		select userid from user where accountID =
		#{accountID} limit 1
	</select>
</mapper>