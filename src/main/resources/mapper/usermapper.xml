<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.netease.shijin.yitao.mapper.UserMapper">
	<select id="isUserExist" resultType="int">
		select 1 from user where accountID = #{accountID} limit 1
	</select>
	<update id="updateUserInfo">
		update user set nickname = #{nickname}, city = #{city} where accountID = #{accountID}
	</update>
	<insert id="addUser">
		insert into
		user(nickname,accountid,iconurl,city,registertime)
		values(#{nickname},#{accountid},#{iconurl},#{city},current_timestamp())
	</insert>
	<select id="getUserID" resultType="string">
		select userID from user where accountID = #{accountID} limit 1
	</select>
</mapper>